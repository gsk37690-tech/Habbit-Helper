# QuestLife workspace

- Preserve the QuestLife fantasy productivity visual language when extending the UI.
- Keep user-facing interactions usable on desktop and mobile.
- Prefer focused client-side interactions for the MVP; introduce backend persistence behind existing UI contracts.
- Run `npm run build` after changes to the dashboard.
